<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 企业微信机器人 ID
    |--------------------------------------------------------------------------
    |
    | 在企业微信管理后台创建 AI 机器人后获取
    |
    */
    'bot_id' => env('WECOM_BOT_ID', ''),

    /*
    |--------------------------------------------------------------------------
    | 企业微信机器人 Secret
    |--------------------------------------------------------------------------
    |
    | 在企业微信管理后台创建 AI 机器人后获取
    |
    */
    'secret' => env('WECOM_BOT_SECRET', ''),

    /*
    |--------------------------------------------------------------------------
    | WebSocket 连接地址
    |--------------------------------------------------------------------------
    |
    | 默认为企业微信官方地址，一般不需要修改
    |
    */
    'ws_url' => env('WECOM_WS_URL', 'wss://openws.work.weixin.qq.com'),

    /*
    |--------------------------------------------------------------------------
    | 心跳间隔（秒）
    |--------------------------------------------------------------------------
    */
    'heartbeat_interval' => env('WECOM_HEARTBEAT_INTERVAL', 30),

    /*
    |--------------------------------------------------------------------------
    | 最大重连次数
    |--------------------------------------------------------------------------
    |
    | 设为 -1 表示无限重连
    |
    */
    'max_reconnect_attempts' => env('WECOM_MAX_RECONNECT', 100),

    /*
    |--------------------------------------------------------------------------
    | 消息处理器类
    |--------------------------------------------------------------------------
    |
    | 指定一个类来处理收到的消息，该类需实现以下方法（均为可选）：
    |
    | - onMessage(Message $message, Reply $reply): void
    | - onText(Message $message, Reply $reply): void
    | - onImage(Message $message, Reply $reply): void
    | - onVoice(Message $message, Reply $reply): void
    | - onFile(Message $message, Reply $reply): void
    | - onEvent(Event $event, Reply $reply): void
    |
    */
    'handler' => env('WECOM_HANDLER', null),
];
